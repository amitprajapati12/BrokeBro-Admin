import 'package:flutter/material.dart';

class OfferApprovalScreen extends StatelessWidget {
  const OfferApprovalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Offer Verification Section (Coming Soon)'),
    );
  }
}
